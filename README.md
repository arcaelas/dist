# Dist

Multimedia resources of the organization: `banner/`, `brand/` and `logo/`. Consume them through raw URLs, for example:

```
https://raw.githubusercontent.com/arcaelas/dist/main/banner/svg/dark.svg
```
